# Growing Me 录制前 Flow QA

日期：2026-09-19  
范围：完整虚构教学案例（Example）与新用户空白起点（Blank）  
证据边界：本备忘录依据当前页面源码、案例 JSON 与现有 QA 记录；**本轮尚未重新完成全路径浏览器录制验收**。下文“源码已修”只表示当前文件中的实现或文案已核对，不等于浏览器 PASS。录制者逐项实走后再把 `REVIEW` 改为 `PASS`；旧版截图或旧 `demo=full` 测试不替代当前入口验证。

## 一句话判断

Example 展示“这样一张地图可以怎样组织和回看”，Blank 展示“一个人真正开始时什么都还没有”。两者不是同一个人的前后对照；Example 是独立编写的虚构情境，不是私人资料的脱敏版，里面的三件当月行动及记录也不是已验证的真实结果。

**已做的源文件检查（PASS，非浏览器验收）：** `examples/teaching-life-grid-monthly.json` 通过当前月度模型的结构校验，含 8 个房间、64 个方向、3 件当月行动；中心、房间、方向的 `source_quote` 均为空。结构与空引用不自动证明全部内容安全或实际交互可用，录制前仍需逐屏核对。

## 录制前的最短使用路径

| 步骤 | 观众此刻要理解什么 | 镜头动作与可见结果 | 状态 |
| --- | --- | --- | --- |
| 1. 入口 | 我看到的是案例还是空白起点？ | 分别打开 `./monthly/?mode=example&view=grid`、`./monthly/?mode=blank&view=grid`；首屏应明确标注“虚构教学案例”或“空白起点”，不可让观众误以为 Example 是作者本人 | REVIEW：需浏览器核验首屏标签与隐私文案 |
| 2. 选择视图 | 九宫格与长廊是同一份内容的两种看法 | Example 从九宫格切到“房间长卷”，再回到九宫格；中心和房间名称应一致。Blank 在没有内容时不应偷放 Example 的房间 | REVIEW：需实测视图切换与空态 |
| 3. 进入房间 | 一级房间可以继续展开长期方向 | Example 打开一扇门，滚动经过八个方向；方向可以点开，不应一闪而过。Blank 若无房间，应解释“等待逐问形成”，不伪装完成度 | REVIEW：需实测滚动、目标门与空态 |
| 4. 打开方向 | 64 个方向是坐标，不是 64 项待办 | Example 打开一个方向，依次看愿景、输入、实践、输出、回看与当月承接；只指出本月三件行动是示意，不宣称虚构记录为真人证据 | REVIEW：需实测工作台内容、可读性与案例标识 |
| 5. 退出与恢复 | 中断后能回到刚才的位置 | 关闭方向工作台应回到原方向；房间终点可直接走出，并回到原房门前；再切换九宫格不应跳错房间 | 源码有返回逻辑；当前录制路径仍为 REVIEW |
| 6. 下一次回看 | 记下来的内容以后如何回来 | Example 重新打开同一方向，应看到同一组**虚构示例**；Blank 在新浏览器中从空白开始，手工导入虚构测试 JSON 后刷新，应只看到 Blank 专用的当前浏览器暂存，并可导出。长期跨月 AI 回流不在本次录制的已验证范围 | REVIEW：需实测刷新、导入、导出；长期效果 NOT_RUN |

## 高优先级录制阻断项

1. **High · 案例真实性边界。** 画面或口播只要出现“真实案例”“脱敏用户”“真实效果”“已经长期监督”等说法，就会把教学演示伪装成结果证据。统一使用“独立编写的虚构教学情境”“运行方式示意”。
2. **High · 导入后数据归属。** Blank 在首次打开时为空，但导入后会暂存在当前浏览器。不能称它始终只读，也不能说页面会自动同步私人文件；Personal 私有文件由本地 coding agent 在确认后更新。
   页面“复制给 Coding Agent”的指令已明确区分：有本地文件权限时走 Personal；无文件权限、经确认得到完整 JSON 时，才走 Blank 手工导入。录制时仍需实测复制出的全文与导入后的提示，不能说复制后 Blank 会自动长出内容。
3. **Medium · 恢复路径。** 方向工作台、房间长卷和九宫格连续切换后，应实际确认焦点、滚动位置和房门没有意外跳走；源码存在恢复处理不等于浏览器 PASS。
4. **Medium · 结果结构。** 打开方向后先让观众看见“想靠近的状态”和四步循环，再展示细节。不要先用 64 格或动画压过核心价值：用户只需选少数当月行动，并留下有来源的记录。

## 页面文案与状态（源码修复仍需浏览器回归）

| 位置 | 发现与建议 | 严重度 | 当前状态 |
| --- | --- | --- | --- |
| Example 模式提示与顶部流程 | 首屏已写“虚构案例 · 非真人资料”，流程已写“示例记录回到方向”；录屏核对实际首屏和深链接仍为示例语气 | High | 源码已修；浏览器 REVIEW |
| 导入对话框 | 原“先显示变化”已换成“检查格式与内容数量”；录屏核对确认前确实不替换当前地图 | High | 源码已修；浏览器 REVIEW |
| Example 方向工作台 | 标题“示例记录”、页脚“本案例的行动与记录均为虚构”已按模式加入；核对打开不同方向后持续正确 | Medium | 源码已修；浏览器 REVIEW |
| Blank 的空白承诺 | 首屏和导入路径已区分“空白起点”“当前浏览器导入”；核对刷新后只恢复 Blank 自己的暂存，不误导为云同步 | Medium | 源码已修；浏览器 REVIEW |
| Blank 的复制入口 | 启动指令已明确“有本地文件权限 → Personal；无文件权限、确认完整 JSON → Blank 手工导入”，页面流程也已改为“本地同步或手动导入”；需实际复制核对 | Medium | 源码已修；浏览器 REVIEW |
| `public/index.html` 的无脚本回退 | `meta refresh`、脚本与可点击链接现均指向 `monthly/?mode=example&view=grid`；仍需浏览器打开入口核对 | Polish | 源码已修；浏览器 REVIEW |

## 录制完成的判定

- 两条入口各留一段无剪辑走通录屏；记下 URL、日期、浏览器、视口和使用的案例文件版本。
- Example：完成九宫格 → 长廊 → 房间 → 方向 → 退出 → 再打开，并全程可辨“虚构”。
- Blank：首次空白 → 复制给 Coding Agent 的本地 Personal 入口 → 可选的**虚构测试 JSON** 手工导入 → 当前浏览器回看 → 导出；不把 Personal 的自动同步错误归因于 Blank，也不在视频里输入私人内容。
- 如果某一步未走通，就在发布材料中保留 `REVIEW` 或 `NOT_RUN`，不要靠剪辑制造已经闭环的印象。

## 下一步，不影响本次录制

- **Now：** 对源码已修的文案与双入口连续路径做实际浏览器回归，保留无剪辑证据。
- **Next：** 用两位非作者用户测试“第一次听懂与第一次找回方向”，观察他们是否需要作者解释。
- **Later：** 再考虑跨月历史、自动回流与外部项目绑定；这些不是当前两版页面的既有能力。

基线机器可读文件：[`recording-flow-baseline-2026-09-19.json`](recording-flow-baseline-2026-09-19.json)。
