# Growing Me 录制前 Flow QA

日期：2026-09-20（沿用 2026-09-19 Flow 基线）

范围：公开开始页、完整虚构教学案例（Example）与新用户空白起点（Blank）

证据边界：2026-09-20 的本机静态预览已实测公开开始页展示与复制 Prompt、Example 独立入口和月页复制 Prompt、Blank 首次空白、Example 九宫格方向工作台的输入试填及重置，记为 **PASS（定向桌面路径）**；此前的房间 → 工作台、目标/输入/证据线索试填及本月行动添加编辑也做过定向桌面验证。当前自动测试 **16/16 PASS**，Skill 校验与 ZIP 解压 PASS。这些仍不是完整无剪辑录制验收；移动端 **NOT_RUN**。`file://` 直开在浏览器控制中被阻断，记为 **BLOCK**；录制应使用公开 Pages 链接或 `./start` 提供的本机地址。

## 一句话判断

Example 展示“这样一张地图可以怎样组织、试填和回看”：打开房间里的方向工作台，访客可临时修改或补入各板块内容，本月行动也可试填；刷新或恢复后回到原始案例，“导出原始案例”不包含试填。Blank 展示“一个人真正开始时什么都还没有”。两者不是同一个人的前后对照；Example 是独立编写的虚构情境，不是私人资料的脱敏版，里面的三件预置当月行动及记录也不是已验证的真实结果。

**已做的源文件检查（PASS，非浏览器验收）：** `examples/teaching-life-grid-monthly.json` 通过当前月度模型的结构校验，含 8 个房间、64 个方向、3 件当月行动；中心、房间、方向的 `source_quote` 均为空。结构与空引用不自动证明全部内容安全或实际交互可用，录制前仍需逐屏核对。

## 录制前的最短使用路径

| 步骤 | 观众此刻要理解什么 | 镜头动作与可见结果 | 状态 |
| --- | --- | --- | --- |
| 1. 入口 | 我看到的是案例还是空白起点？ | 公开根页先给 Example、Prompt、Skill 三条路径；录制直接打开独立 Example 链接，再给 Blank 链接。Example 首屏须标明虚构，Blank 首屏保持空白 | PASS：本机预览三入口；REVIEW：发布后线上与完整录制 |
| 2. 选择视图 | 九宫格与长廊是同一份内容的两种看法 | Example 从九宫格切到“房间长卷”，再回到九宫格；中心和房间名称应一致。Blank 在没有内容时不应偷放 Example 的房间 | REVIEW：需实测视图切换与空态 |
| 3. 进入房间 | 一级房间可以继续展开长期方向 | Example 打开一扇门，点开一个方向工作台；完整录制仍需滚动经过八个方向。Blank 若无房间，应解释“等待逐问形成”，不伪装完成度 | PASS：桌面房间打开一个方向；REVIEW：八方向长卷与 Blank 空态 |
| 4. 试填方向工作台 | 每个方向板块能改、能补入，而不改变原始案例 | 点击“试填这份工作台”，在目标、输入、前进证据描述分别改写，保存后回到工作台并查看房间卡片更新；录制时再走一遍反面校准、实践、输出、回看等板块。前进证据描述不等于真人已完成记录 | PASS：桌面目标/输入/前进证据描述、保存及房间卡片更新；REVIEW：其余字段逐项录制 |
| 5. 试填本月行动 | 长期方向与少量当月行动分层 | 从工作台或房间的月度区添加一件事，再打开编辑；只把它称为当前页面试填，不称为真实进展或持久保存 | PASS：桌面添加、编辑；REVIEW：完整录制中的叙事与标签 |
| 6. 恢复原始案例 | 试填不是个人地图或可导出的成果 | 点击“恢复原始案例”，再试填并刷新；原始虚构内容应两次恢复。“导出原始案例”不应包含试填，不能拿它当试填备份 | PASS：桌面恢复与刷新；REVIEW：手工核对导出文件 |
| 7. 退出与返回 | 中断后能回到刚才的位置 | 关闭方向工作台应回到原方向；房间终点可直接走出，并回到原房门前；再切换九宫格不应跳错房间 | REVIEW：完整连续录制仍未重新走通 |
| 8. 给观众的使用入口 | Prompt 可立即开始对话，Skill 才负责本地持续保存规则 | 从独立 Example 链接到公开开始页，复制 Prompt、查看 Skill ZIP 与 GitHub 克隆步骤；不要说复制后网站自动长出内容 | PASS：本机预览复制状态和链接；REVIEW：实际粘贴内容、线上点击与无剪辑口播 |
| 9. Blank 下一次回看 | 新用户从空白起步，导入后仅在本浏览器暂存 | Blank 在新浏览器中从空白开始，手工导入虚构测试 JSON 后刷新，应只看到 Blank 专用的当前浏览器暂存，并可导出。长期跨月 AI 回流不在本次录制的已验证范围 | PASS：首次空白；REVIEW：刷新、导入、导出；长期效果 NOT_RUN |

## 高优先级录制阻断项

1. **High · 案例真实性边界。** 画面或口播只要出现“真实案例”“脱敏用户”“真实效果”“已经长期监督”等说法，就会把教学演示伪装成结果证据。统一使用“独立编写的虚构教学情境”“运行方式示意”。
   工作台里的试填与预置案例记录必须区分；“前进证据”可写判断标准，不可把试填文字说成已发生的真实记录。
2. **High · 导入后数据归属。** Blank 在首次打开时为空，但导入后会暂存在当前浏览器。不能称它始终只读，也不能说页面会自动同步私人文件；Personal 私有文件由本地 coding agent 在确认后更新。
   页面现在提供“复制访谈 Prompt”和“获取完整 Skill”；两处复制按钮读取同一份 Prompt。无本地权限时可先聊，但只有取得完整当前地图与 Schema 且经确认，才可交付可导入 Blank 的合法 JSON。录制时仍需实测粘贴全文与导入提示，不能说复制后 Blank 会自动长出内容。
3. **High · 试填不保存。** Example 的方向和行动试填只留在当前页面内存；刷新或恢复即清除，“导出原始案例”也不带走试填。录制不要填写个人敏感信息，也不要称导出为试填备份。
4. **Medium · 恢复路径。** 方向工作台、房间长卷和九宫格连续切换后，应实际确认焦点、滚动位置和房门没有意外跳走；定向编辑 PASS 不等于整段录制 PASS。
5. **Medium · 入口与视口。** `file://` 直开为 BLOCK；从公开 Pages Example 链接或 `./start` 的本机服务地址录制。移动视口覆盖未生效，移动端 NOT_RUN，不能据桌面 PASS 宣称移动端已验收。
6. **Medium · 结果结构。** 打开方向后先让观众看见“想靠近的状态”和四步循环，再展示细节。不要先用 64 格或动画压过核心价值：用户只需选少数当月行动，并留下有来源的记录。

## 页面文案与状态（源码修复仍需浏览器回归）

| 位置 | 发现与建议 | 严重度 | 当前状态 |
| --- | --- | --- | --- |
| Example 模式提示与顶部流程 | 首屏已写“虚构案例 · 非真人资料”，流程已写“示例记录回到方向”；录屏核对实际首屏和深链接仍为示例语气 | High | 源码已修；浏览器 REVIEW |
| 导入对话框 | 原“先显示变化”已换成“检查格式与内容数量”；录屏核对确认前确实不替换当前地图 | High | 源码已修；浏览器 REVIEW |
| Example 方向工作台 | “试填这份工作台”可改、可补入各板块，提示刷新还原；目标/输入/前进证据描述的桌面试填及房间卡片更新已实测。还需逐项录制其余字段与不同方向 | High | 定向桌面 PASS；其余字段录制 REVIEW |
| Example 导出与恢复 | 按钮为“导出原始案例”；恢复/刷新已实测回到原始案例，导出文件仍需人工核对不含试填 | High | 恢复/刷新 PASS；手工导出 REVIEW |
| Blank 的空白承诺 | 首屏和导入路径已区分“空白起点”“当前浏览器导入”；核对刷新后只恢复 Blank 自己的暂存，不误导为云同步 | Medium | 源码已修；浏览器 REVIEW |
| Blank 的复制入口 | “复制访谈 Prompt”从独立文件读取；无仓库也能先聊，但不能自动写网站。Blank 首次空白在本机静态预览可见；需核对实际粘贴与手工导入 | Medium | 复制状态/空白 PASS；实际粘贴、导入 REVIEW |
| `public/index.html` 开始页 | 根页不再跳转 Example；有独立的 Example、Prompt、Skill/仓库及 Blank 链接。复制失败可展开手动复制文本 | Polish | 本机预览视觉/复制状态 PASS；线上与失败降级 REVIEW |

## 录制完成的判定

- 两条入口各留一段无剪辑走通录屏；记下 URL、日期、浏览器、视口和使用的案例文件版本。
- Example：完成九宫格 → 长廊 → 房间 → 方向工作台 → 修改已有板块、补入空白板块 → 可选试填本月行动 → 退出/重开 → 恢复或刷新，并全程可辨“虚构”“试填不保存”。导出按钮应显示“导出原始案例”，手工核对导出的仍是未改动的虚构样本。
- 结尾：从 Example 长廊尽头或单独打开开始页 → 复制访谈 Prompt → 展示 GitHub Skill/克隆步骤。说明仅聊天可先探索，长期保存需本地仓库、`./start`、确认写入。
- Blank：首次空白 → 复制访谈 Prompt → 可选的**虚构测试 JSON** 手工导入 → 当前浏览器回看 → 导出；不把 Personal 的自动同步错误归因于 Blank，也不在视频里输入私人内容。
- 如果某一步未走通，就在发布材料中保留 `REVIEW` 或 `NOT_RUN`，不要靠剪辑制造已经闭环的印象。
- 移动端在真实移动视口重新走工作台表单前保持 `NOT_RUN`；不要把这次未生效的视口覆盖当成移动端 PASS。`file://` 直开为 `BLOCK`，不作为录制入口。

## 下一步，不影响本次录制

- **Now：** 从公开 Pages Example 链接补做完整无剪辑录屏、粘贴复制内容、Example 其余工作台字段及原始导出文件核对；用确实生效的移动视口重测表单。定向桌面 PASS 和 16/16 自动测试不能替代这些步骤。
- **Next：** 用两位非作者用户测试“第一次听懂与第一次找回方向”，观察他们是否需要作者解释。
- **Later：** 再考虑跨月历史、自动回流与外部项目绑定；这些不是当前两版页面的既有能力。

基线机器可读文件：[`recording-flow-baseline-2026-09-19.json`](recording-flow-baseline-2026-09-19.json)。
